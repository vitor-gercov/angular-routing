import { Routes } from "@angular/router";
import { BasicChildComponent } from "./basic-child.component";

export const basicChildRoutes: Routes = [
    {
        path: '',
        component: BasicChildComponent
    }
]